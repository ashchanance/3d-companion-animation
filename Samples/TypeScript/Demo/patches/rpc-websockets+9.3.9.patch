diff --git a/node_modules/rpc-websockets/dist/index.cjs b/node_modules/rpc-websockets/dist/index.cjs
index fe29b26..54d753e 100644
--- a/node_modules/rpc-websockets/dist/index.cjs
+++ b/node_modules/rpc-websockets/dist/index.cjs
@@ -3,7 +3,7 @@
 var WebSocketImpl = require('ws');
 var eventemitter3 = require('eventemitter3');
 var url = require('url');
-var uuid = require('uuid');
+var crypto = require('crypto');
 
 function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }
 
@@ -371,7 +371,7 @@ var Server = class extends eventemitter3.EventEmitter {
       const u = url__default.default.parse(request.url, true);
       const ns = u.pathname;
       if (u.query.socket_id) socket._id = u.query.socket_id;
-      else socket._id = uuid.v1();
+      else socket._id = typeof crypto.randomUUID === "function" ? crypto.randomUUID() : `${Date.now()}-${Math.random().toString(16).slice(2)}`;
       socket["_authenticated"] = false;
       socket.on("error", (error) => this.emit("socket-error", socket, error));
       socket.on("close", () => {
